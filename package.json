{
  "name": "resume-copilot",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ai-resume-frontend run dev -- --host 127.0.0.1 --port 3000",
    "dev:frontend": "npm --prefix ai-resume-frontend run dev -- --host 127.0.0.1 --port 3000",
    "dev:backend": "npm --prefix ai-resume-backend run start"
  }
}
